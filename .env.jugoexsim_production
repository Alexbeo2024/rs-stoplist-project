# =====================================
# JUGOEXSIM EMAIL CONFIGURATION (PRODUCTION)
# =====================================

# Email настройки (ПРОВЕРЕНО!)
EMAIL_USER=aak@jugoexsim.rs
EMAIL_PASS=YOUR_ACTUAL_PASSWORD_HERE

# SMTP Settings (SSL на порту 465) - РАБОТАЕТ!
SMTP_SERVER=smtp.jugoexsim.rs
SMTP_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True

# IMAP Settings (используем mail.jugoexsim.rs - РАБОТАЕТ!)
IMAP_SERVER=mail.jugoexsim.rs
IMAP_PORT=993

# SSL настройки для корпоративного сервера
EMAIL_SSL_VERIFY=False
EMAIL_TIMEOUT=30

# Настройки обработки
EMAIL_CHECK_INTERVAL=3600  # Проверка каждый час
EMAIL_ALLOWED_SENDERS=sender@jugoexsim.rs,reports@jugoexsim.rs

# =====================================
# ОСТАЛЬНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ
# =====================================

# Database Configuration
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=emailprocessor
POSTGRES_PASSWORD=secure_password_123
POSTGRES_DB=email_processor_db

# SFTP Configuration
SFTP_HOST=your-sftp-server.com
SFTP_USER=your-sftp-user
SFTP_PASS=your-sftp-password

# Notifications
TG_BOT_TOKEN=your-telegram-bot-token
TG_CHAT_ID=your-telegram-chat-id

# Security
SECRET_KEY=generate-strong-secret-key-for-production
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
